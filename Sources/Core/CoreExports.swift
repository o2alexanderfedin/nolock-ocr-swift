// Core/CoreExports.swift - Re-exports core types

// Export Core types
// These are all part of the same module, so we don't need to import them

// Re-export ClientEnvironment (already used in OCRClient)
// Re-export EndpointBuilder (new utility class)
// Re-export ImageProcessor (new utility class for image format handling)